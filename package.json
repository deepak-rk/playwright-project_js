{
  "name": "mobile-hub-e2e",
  "version": "0.1.0",
  "private": true,
  "description": "End-to-end tests for mobile-hub (API + UI) using Playwright.",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "report": "playwright show-report"
  },
  "license": "MIT",
  "devDependencies": {
    "@playwright/test": "^1.46.0",
    "@types/node": "^20.12.12",
    "@types/ws": "^8.5.10",
    "mongodb": "^6.8.0",
    "typescript": "^5.4.5",
    "ws": "^8.18.0"
  }
}
